{
  "name": "spotify-test",
  "private": true,
  "scripts": {
    "build": "npm ci --include=dev --prefix frontend && npm run build --prefix frontend && node scripts/copy-frontend.js && npm ci --prefix backend && npm run build --prefix backend",
    "start": "npm run start:prod --prefix backend"
  }
}
